; =====================================================================
;  Docker-specific FPM overrides
; =====================================================================

[global]
; Send FPM logs to stderr (Docker captures them)
error_log = /proc/self/fd/2
log_limit = 8192
